# NGX Analyser

![License: MIT](https://img.shields.io/badge/License-MIT-00C896.svg)

A Streamlit-based technical analysis dashboard for equities listed on the **Nigerian Exchange (NGX)**. Data is served from a MongoDB Atlas collection populated by the NGX crawler. Indicators are computed in pure pandas at query time; trading signals are generated by rule-based crossover strategies; AI recommendations are produced by your choice of Claude, GPT-4o, or Gemini via LangChain.

---

## Screenshots

### Market Overview

![Market Overview](images/landingPage.png)

### Sector Overview

![Sector Overview](images/sectorOverview.png)

### Stock Deep Dive

![Stock Deep Dive](images/deep_dive.png)

### AI Recommendation

![AI Recommendation](images/ai_recommendation.png)

---

## Architecture

```
app.py  (st.set_page_config + sidebar router + auth gate)
  │
  ├── views/login.py
  ├── views/admin.py
  │
  ├── views/market_overview.py  ──► charts/market_overview.py  (Plotly figs)
  │         └── data/loader.py  ──► MongoDB Atlas (tradesData_clean)
  │
  └── views/deep_dive.py        ──► charts/deep_dive.py        (Plotly figs)
            ├── data/loader.py
            ├── data/indicators.py       (pure pandas)
            ├── analysis/signals.py      (crossover strategies)
            └── analysis/recommender.py  (LangChain + Claude / GPT-4o / Gemini)
```

**Layer rules:**

- `views/` — Streamlit rendering only; no chart construction, no raw queries
- `charts/` — accept DataFrames, return `go.Figure`; no `st.*` calls
- `data/loader.py` — all MongoDB queries; every public function wrapped in `@st.cache_data`
- `data/indicators.py` — stateless functions only; no I/O
- `analysis/signals.py` — every strategy returns `(buy_df, sell_df)` and is registered in `STRATEGIES` for UI dispatch
- `utility/auth.py` — MongoDB-backed user auth with PBKDF2-HMAC password hashing
- `config.py` — single source of truth for colours and `PLOTLY_BASE` layout defaults

---

## Authentication

The app ships with a login gate backed by MongoDB (`users` collection). On first run, `init_users_collection()` creates a default admin account:

| Field | Example |
| --- | --- |
| Username | `admin` (set via `ADMIN_USERNAME` in `.env`) |
| Password | `admin123` (set via `ADMIN_PASSWORD` in `.env`) |
| Role | `admin` |

Admins can create, activate/deactivate, and manage users from the **Admin Panel** page.

---

## Technical Indicators

All computed in `data/indicators.py` against `ClosePrice`:

| Indicator | Function | Parameters |
| --- | --- | --- |
| SMA | `calculate_sma(series, window)` | 50, 200 |
| EMA | `calculate_ema(series, window)` | 12, 26 |
| RSI | `calculate_rsi(series, window)` | 14, 7 |
| MACD | `calculate_macd(series, short, long, signal)` | 12, 26, 9 |

`add_all_indicators(df)` appends all of the above as new columns to a copy of the OHLCV DataFrame.

---

## Trading Strategies

Defined in `analysis/signals.py`. Each function returns `(buy_df, sell_df)`.

| Strategy | Buy condition | Sell condition |
| --- | --- | --- |
| `macd_signals` | MACD crosses above Signal line | MACD crosses below Signal line |
| `rsi_signals` | RSI crosses up through oversold (30) | RSI crosses down through overbought (70) |
| `sma_crossover_signals` | SMA 50 crosses above SMA 200 (golden cross) | SMA 50 crosses below SMA 200 (death cross) |

---

## Database

MongoDB Atlas collection (populated and cleaned by the NGX crawler pipeline).

### `Collection`

| Field | Type | Notes |
| --- | --- | --- |
| `Symbol` | string | NGX ticker |
| `TradeDate` | string | ISO date string |
| `fetched_at` | datetime | WAT-aware timestamp; used as the primary time axis |
| `OpeningPrice` | float | |
| `HighPrice` | float | |
| `LowPrice` | float | |
| `ClosePrice` | float | Used for all indicator calculations |
| `PrevClosingPrice` | float | |
| `Change` | float | Absolute day change |
| `PercChange` | float | Percentage change |
| `CalculateChangePercent` | float | |
| `Volume` | float | |
| `Value` | float | NGN value traded |
| `Trades` | float | Number of trades |
| `Market` | string | |
| `Sector` | string | |
| `Company2` | string | Company display name |

Unique index on `(Symbol, fetched_at)`.

---

## AI Recommendations

`analysis/recommender.py` builds a LangChain chain and supports three providers:

```python
chain = PromptTemplate(...) | llm | StrOutputParser()
```

| Provider | Model |
| --- | --- |
| Claude (Anthropic) | `claude-opus-4-7` |
| OpenAI (GPT-4o) | `gpt-4o` |
| Gemini (Google) | `gemini-1.5-pro` |

The prompt passes buy/sell signals and stock details. The model returns a `Recommendation: BUY / SELL / HOLD` verdict with a brief reason. The result is cached in `st.session_state` until the user clicks **Refresh**.

---

## Tech Stack

| Layer | Library |
| --- | --- |
| UI | `streamlit >= 1.35` |
| Charts | `plotly >= 5.22` |
| Data | `pandas >= 2.0`, `pymongo >= 4.0` |
| Indicators | Pure pandas — no TA-Lib |
| AI | `langchain-anthropic`, `langchain-openai`, `langchain-google-genai` |
| Auth | `pymongo` + `hashlib` |
| Config | `python-dotenv >= 1.0`, `pytz` |

---

## Setup

### 1. Create a virtual environment

```bash
cd platformApp
python -m venv venv
source venv/Scripts/activate      # Windows bash
# or: venv\Scripts\activate.bat
```

### 2. Install dependencies

```bash
pip install -r requirements.txt
```

### 3. Configure environment

Create `platformApp/.env`:

```env
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/
MONGODB_DB=db_name_here
ADMIN_USERNAME=username_here
ADMIN_PASSWORD=password_here
ANTHROPIC_API_KEY=your_key_here
OPENAI_API_KEY=your_key_here
GEMINI_API_KEY=your_key_here
```

### 4. Run

```bash
streamlit run app.py
# → http://localhost:8501
```

---
